google-genai>=1.0.0
openai>=1.0.0
anthropic>=0.40.0

# Phase 9: httpx is used for OpenAI client timeout configuration (agent.py line 48).
# Must be present so the lazy import succeeds; pin to a range that tracks security releases.
httpx>=0.27.0

# Phase 1 (UnifiedClaw)
websockets>=12.0  # Phase 1: FitnessReporter WSBridge connection
# sqlite-vec>=0.1.0  # Phase 1: optional vector search (uncomment to enable)
